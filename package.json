{
  "name": "ds-tailwind-theme",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "app": "cd demo-app && npm install && npm start",
    "demo": "cd demo-site && npm install && npm run dev",
    "start": "cd demo-site && npm install && npm run dev",
    "build": "cd demo-site && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.6"
  },
  "dependencies": {
    "@metamask/design-tokens": "^4.0.0"
  }
}
