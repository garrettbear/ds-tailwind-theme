{
  "name": "ds-tailwind-theme",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "demo": "cd demo-site && npm install && npm run dev",
    "build": "npm --prefix demo-site run build",
    "dev": "npm --prefix demo-site run dev",
    "start": "npm --prefix demo-site run start",
    "lint": "npm --prefix demo-site run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.6"
  }
}
